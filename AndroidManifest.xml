<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gamebooster.real">

    <!-- ===== IZIN PENTING UNTUK RAM CLEANER ===== -->
    
    <!-- Izin untuk mematikan proses background (WAJIB) -->
    <uses-permission android:name="android.permission.KILL_BACKGROUND_PROCESSES" />
    
    <!-- Izin untuk mendapatkan daftar proses yang berjalan -->
    <uses-permission android:name="android.permission.GET_TASKS" />
    <uses-permission android:name="android.permission.REAL_GET_TASKS" />
    
    <!-- Izin untuk menampilkan overlay di atas game -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    
    <!-- Izin untuk membaca status baterai -->
    <uses-permission android:name="android.permission.BATTERY_STATS" />
    
    <!-- Izin untuk membaca penyimpanan -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    
    <!-- Untuk mendeteksi aplikasi yang terinstall (Android 11+) -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    
    <!-- Izin internet untuk WebView -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    
    <!-- Query package untuk game populer -->
    <queries>
        <package android:name="com.mobile.legends" />
        <package android:name="com.dts.freefireth" />
        <package android:name="com.dts.freefiremax" />
        <package android:name="com.tencent.ig" />
        <package android:name="com.miHoYo.GenshinImpact" />
        <package android:name="com.activision.callofduty.shooter" />
        <package android:name="com.ea.gp.fifamobile" />
        <package android:name="com.innersloth.spacemafia" />
        <package android:name="com.riotgames.league.wildrift" />
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Game Booster Real"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GameBoosterReal"
        android:usesCleartextTraffic="true">
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <service 
            android:name=".FloatingViewService"
            android:exported="false"
            android:enabled="true" />
        
    </application>

</manifest>
